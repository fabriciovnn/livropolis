import { styled } from "styled-components";

const InfosStyled = styled.div`
  max-width: 100%;
  font-size: 1.2rem;

  &::placeholder {
    color: #313131; /* Substitua pela cor desejada */
  }
`;

export default InfosStyled;
